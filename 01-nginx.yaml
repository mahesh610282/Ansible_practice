- name: install nginx and run the service
  hosts: web
  become: yes
  tasks:
    - name: install nginx
      ansible.builtin.package:
        name: nginx
        state: present
        update_cache: yes
    - name: start service
      ansible.builtin.service:
        name: nginx
        state: started
        enabled: yes