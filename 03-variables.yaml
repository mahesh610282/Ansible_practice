- name: variables 
  hosts: web
  vars:
    name: "mahesh"
    owner: "Mahesh cheerneni"
  tasks:
    - name: variables Declaration
      ansible.builtin.debug:
        msg: "variables are created {{name}} and {{Owner}}"
        